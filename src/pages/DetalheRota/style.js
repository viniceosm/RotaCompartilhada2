import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    descricaoRota: {
        fontSize: 20,
        padding: 5
    },
    map: {
        height: "80%",
        width: "100%",
    }
});

export default styles;